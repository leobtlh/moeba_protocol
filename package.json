{
  "name": "moeba-protocol",
  "version": "1.0.0",
  "scripts": {
    "build": "mkdir -p dist_root && cp -r index.html static img documentation backoffice dist_root/ && cd app && npm install && npm run build && cp -r dist ../dist_root/app"
  },
  "devDependencies": {
    "@nomicfoundation/hardhat-ethers": "^4.0.4",
    "dotenv": "^17.3.1",
    "ethers": "^6.16.0",
    "hardhat": "^3.1.9"
  },
  "dependencies": {
    "@openzeppelin/contracts": "^5.4.0"
  },
  "type": "module"
}
